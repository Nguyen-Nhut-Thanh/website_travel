# Travel Project - API

Travel agency management API built with NestJS and Prisma.

## Setup
1. `npm install`
2. Create `.env` file from `.env.example`
3. `npx prisma generate`
4. `npm run start:dev`
